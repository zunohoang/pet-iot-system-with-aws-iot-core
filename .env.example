# Monorepo root — optional. Prefer:
#   mobile-app/.env     — Expo (API, Cognito, IoT)
#   infra/terraform.tfvars — deploy (sensitive; gitignored)
#   device/*/ — copy values into src/config.h (firmware has no .env at runtime)
#
# No secrets in this file.
