# Local testing / sam local / node -e "process.env=..." — copy to .env (optional)
# In production these are set by Terraform on each Lambda (see infra/modules/processing/lambda.tf).

PROJECT_NAME=iot-smarthome
AWS_REGION_NAME=ap-southeast-1
IOT_DATA_ENDPOINT=xxxxxxxx-ats.iot.ap-southeast-1.amazonaws.com

DYNAMODB_DEVICES_TABLE=
DYNAMODB_CLAIMS_TABLE=
DYNAMODB_SCENES_TABLE=
DYNAMODB_OTA_JOBS_TABLE=
USER_DEVICE_TABLE=

FIRMWARE_BUCKET=
SNS_ALERT_TOPIC_ARN=

INFLUXDB_URL=
INFLUXDB_ORG=
INFLUXDB_BUCKET=
INFLUXDB_SECRET_ARN=arn:aws:secretsmanager:ap-southeast-1:000000000000:secret:...
